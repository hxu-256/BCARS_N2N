# Versions from the GPU container used for the paper's N2N runs
# (locally built image "pytorch-tensorflow-gpu", nvidia/cuda 12.8 + Ubuntu 24.04,
#  built 2025-09-19). Python 3.11.13.
#
# NOTE: the deep-learning stack was installed from nightly builds, which are
# eventually removed from their indexes. Pin the nearest stable release if the
# exact version below is no longer downloadable; nothing here depends on a
# nightly-only feature.
csbdeep==0.8.1
tf_nightly==2.21.0.dev20250916      # stable equivalent: tensorflow~=2.20
keras-nightly==3.12.0.dev2025091903 # stable equivalent: keras~=3.11
numpy==2.3.3
scipy==1.16.2
h5py==3.14.0
tifffile==2025.10.4
matplotlib==3.10.6
